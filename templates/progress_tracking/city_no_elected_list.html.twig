{% extends 'base.html.twig' %}

{% block title %}{{ city.name }} - Aucune liste élue{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="text-center">
                <!-- Breadcrumb -->
                <nav aria-label="breadcrumb" class="mb-4">
                    <ol class="breadcrumb justify-content-center">
                        <li class="breadcrumb-item">
                            <a href="{{ path('app_progress_tracking_index') }}">Suivi des Mandatures</a>
                        </li>
                        <li class="breadcrumb-item active">{{ city.name }}</li>
                    </ol>
                </nav>

                <!-- Icône et titre -->
                <div class="mb-4">
                    <i class="fas fa-info-circle fa-4x text-muted mb-3"></i>
                    <h1 class="h3 mb-2">{{ city.name }}</h1>
                    <h2 class="h5 text-muted">Aucune liste élue enregistrée</h2>
                </div>

                <!-- Message principal -->
                <div class="card border-0 shadow-sm mb-4">
                    <div class="card-body p-4">
                        <h5 class="card-title text-primary mb-3">
                            <i class="fas fa-calendar-alt"></i>
                            En attente des résultats électoraux
                        </h5>
                        <p class="card-text text-muted mb-4">
                            Le suivi des engagements pour <strong>{{ city.name }}</strong> sera disponible 
                            après les élections municipales, une fois qu'une liste aura été élue et 
                            enregistrée dans le système.
                        </p>
                        
                        <div class="row text-center">
                            <div class="col-md-4 mb-3">
                                <div class="p-3">
                                    <i class="fas fa-vote-yea fa-2x text-primary mb-2"></i>
                                    <h6 class="mb-1">Élections</h6>
                                    <small class="text-muted">Attente des résultats</small>
                                </div>
                            </div>
                            <div class="col-md-4 mb-3">
                                <div class="p-3">
                                    <i class="fas fa-user-tie fa-2x text-muted mb-2"></i>
                                    <h6 class="mb-1">Liste élue</h6>
                                    <small class="text-muted">À enregistrer</small>
                                </div>
                            </div>
                            <div class="col-md-4 mb-3">
                                <div class="p-3">
                                    <i class="fas fa-chart-line fa-2x text-muted mb-2"></i>
                                    <h6 class="mb-1">Suivi</h6>
                                    <small class="text-muted">Bientôt disponible</small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Informations sur la commune -->
                {% if city.description %}
                    <div class="card border-0 shadow-sm mb-4">
                        <div class="card-body">
                            <h6 class="card-title">
                                <i class="fas fa-map-marker-alt text-primary"></i>
                                À propos de {{ city.name }}
                            </h6>
                            <p class="card-text text-muted mb-0">{{ city.description }}</p>
                        </div>
                    </div>
                {% endif %}

                <!-- Propositions disponibles -->
                {% if city.candidateLists is not empty %}
                    <div class="card border-0 shadow-sm mb-4">
                        <div class="card-body">
                            <h6 class="card-title">
                                <i class="fas fa-list-alt text-primary"></i>
                                Listes candidates et leurs engagements
                            </h6>
                            <p class="text-muted mb-3">
                                Vous pouvez consulter les engagements pris par les différentes listes candidates 
                                pendant la campagne électorale.
                            </p>
                            <a href="{{ path('app_city_show', {slug: city.slug}) }}" class="btn btn-primary">
                                <i class="fas fa-eye"></i>
                                Voir les engagements de campagne
                            </a>
                        </div>
                    </div>
                {% endif %}

                <!-- Actions -->
                <div class="d-flex justify-content-center gap-3">
                    <a href="{{ path('app_progress_tracking_index') }}" class="btn btn-outline-primary">
                        <i class="fas fa-arrow-left"></i>
                        Retour à l'accueil
                    </a>
                    
                    {% if city.candidateLists is not empty %}
                        <a href="{{ path('app_city_show', {slug: city.slug}) }}" class="btn btn-outline-secondary">
                            <i class="fas fa-list"></i>
                            Voir la commune
                        </a>
                    {% endif %}
                </div>

                <!-- Note informative -->
                <div class="mt-5">
                    <div class="alert alert-light border-0" role="alert">
                        <small class="text-muted">
                            <i class="fas fa-info-circle"></i>
                            <strong>Note :</strong> Le suivi des mandatures permet de suivre la mise en œuvre 
                            des engagements pris par les listes élues. Cette fonctionnalité sera activée 
                            automatiquement après l'enregistrement des résultats électoraux.
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.breadcrumb {
    background: none;
    padding: 0;
}

.card {
    transition: transform 0.2s ease-in-out;
}

.card:hover {
    transform: translateY(-2px);
}

.gap-3 {
    gap: 1rem !important;
}

.alert {
    border-radius: 0.5rem;
}
</style>
{% endblock %}
