<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Plaidoyer Vélo Bordeaux{% endblock %}</title>
        <meta name="description" content="{% block descriptionmeta %}Suivi des engagements de plaidoyer des listes candidates dans les communes de Bordeaux Métropole{% endblock %}">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🤝</text></svg>">

        <link rel="stylesheet" href="{{ asset('styles/app.scss') }}">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
        {% block stylesheets %}
        {% endblock %}

        <!-- Custom styles are loaded via app.js importmap -->
    </head>
    <body>
        <!-- Navigation -->
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top">
            <div class="container">
                <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">
                    <i class="fas fa-bicycle me-2"></i>
                    Plaidoyer Vélo Bordeaux
                </a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_home') }}">
                                <i class="fas fa-home me-1"></i>Accueil
                            </a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-list me-1"></i>Propositions
                            </a>
                            <ul class="dropdown-menu">
                                {% for category in get_all_categories() %}
                                    <li><a class="dropdown-item" href="{{ path('app_category_show', {id: category.id, slug: category.slug}) }}">
                                        {{ category.name }}
                                    </a></li>
                                {% endfor %}
                            </ul>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-city me-1"></i>Communes
                            </a>
                            <ul class="dropdown-menu">
                                {% for city in get_all_cities() %}
                                    <li><a class="dropdown-item" href="{{ path('app_city_show', {slug: city.slug}) }}">
                                        <i class="fas fa-map-marker-alt me-2"></i>{{ city.name }}
                                    </a></li>
                                {% endfor %}
                            </ul>
                        </li>
                        <li>
                            <a href="{{ path('app_collectif') }}" class="nav-link">Le collectif</a>
                        </li>
                        <li>
                            <a href="{{ path('app_maison_des_livreurs') }}" class="nav-link">Maison des livreurs</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        <!-- Main Content -->
        <main>
            {% block body %}{% endblock %}
        </main>

        <!-- Footer -->
        <footer class="bg-dark text-light py-4 mt-5">
            <div class="container">
                <div class="row">
                    <div class="col-md-6">
                        <h5 class="fw-bold">Plaidoyer Vélo Bordeaux</h5>
                        <p class="mb-0">Plateforme de suivi des engagements des listes candidates dans Bordeaux Métropole.</p>
                    </div>
                </div>
            </div>
        </footer>

        {% block javascripts %}
            {{ importmap('app') }}
        {% endblock %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
